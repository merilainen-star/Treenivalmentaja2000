<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    
    <!-- Two uses, both read-only and both user-initiated:
         1. fetching the rolling test build's metadata so Settings can say whether the installed
            build is the current one — data/update/UpdateService.kt;
         2. fetching an exercise guide when a movement is tapped, and nothing is kept afterwards —
            data/guide/ExerciseDbProvider.kt and docs/EXERCISE_GUIDE.md. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- The app installs its own updates through PackageInstaller rather than handing the APK to
         a browser and the Downloads folder, so it has to be allowed to ask. It is only an
         entitlement to *ask*: Android still requires the per-app "Asenna tuntemattomia
         sovelluksia" toggle, and still shows its own confirmation dialog for every install.
         No storage permission accompanies it, because the download never becomes a file the user
         can see — data/update/PackageInstallerApkInstaller.kt. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".TreenivalmentajaApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Starting">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Exported because a browser has to be able to start it, which is what makes it the one
             component in this app that an arbitrary caller can reach. It acts on nothing it is
             given: the URI is handed to OuraConnection, which discards anything whose `state` is
             not the value this device generated. See docs/SECURITY.md. -->
        <activity
            android:name=".OuraCallbackActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="treenivalmentaja" android:host="oauth2callback" />
            </intent-filter>
        </activity>

        <!-- PackageInstaller's status callback. Not exported, and deliberately not routed
             through MainActivity: the callback carries an Intent this app then starts, so an
             exported entry point would let another app hand it one of its own. Only the system
             can reach a non-exported receiver named by an explicit PendingIntent.
             See data/update/UpdateInstallReceiver.kt and docs/SECURITY.md. -->
        <receiver android:name=".data.update.UpdateInstallReceiver" android:exported="false" />

        <receiver android:name=".data.alarm.ReminderReceiver" android:exported="false" />
        <receiver android:name=".data.alarm.ActionReceiver" android:exported="false" />
        <receiver android:name=".data.alarm.BootReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
